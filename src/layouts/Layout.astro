---
import "../styles/global.css";
import { auth } from "../lib/auth";

interface Props {
    title: string;
}

const { title } = Astro.props;

const session = await auth.api.getSession({
    headers: Astro.request.headers,
});

// Redirect to login if not authenticated (except for public pages if any, but this layout is for app pages)
if (!session) {
    return Astro.redirect("/");
}

const user = session.user;

// Helper to get initials
const getInitials = (name: string) => {
    if (!name) return "U";
    const names = name.trim().split(" ");
    if (names.length >= 2) {
        return (names[0][0] + names[names.length - 1][0]).toUpperCase();
    }
    return name.substring(0, 2).toUpperCase();
};

const displayName = user.name || user.email || "Usuario";
const initials = getInitials(displayName);
const isSuperAdmin = (user as any).role === "admin";

const currentPath = Astro.url.pathname;

const isActive = (path: string) => {
    if (path === "/dashboard" && currentPath === "/dashboard") return true;
    if (path !== "/dashboard" && currentPath.startsWith(path)) return true;
    return false;
};
---

<!doctype html>
<html lang="es">
    <head>
        <meta charset="UTF-8" />
        <meta
            name="description"
            content="LaricoWallet - Tu billetera financiera"
        />
        <meta name="viewport" content="width=device-width" />
        <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
        <meta name="generator" content={Astro.generator} />
        <title>{title} | LaricoWallet</title>
    </head>
    <body class="bg-gray-50 dark:bg-slate-900 transition-colors duration-300">
        <!-- Header -->
        <header
            class="bg-white dark:bg-slate-800 shadow-sm border-b border-gray-200 dark:border-slate-700 sticky top-0 z-40"
        >
            <div class="max-w-8xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex justify-between items-center h-16">
                    <!-- Left side: Sidebar toggle and Logo -->
                    <div class="flex items-center space-x-4">
                        <button
                            id="mobile-menu-btn"
                            class="lg:hidden p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-slate-700"
                        >
                            <svg
                                class="w-6 h-6"
                                fill="none"
                                stroke="currentColor"
                                viewBox="0 0 24 24"
                            >
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M4 6h16M4 12h16M4 18h16"></path>
                            </svg>
                        </button>
                        <a
                            href="/dashboard"
                            class="flex items-center space-x-2"
                        >
                            <div
                                class="w-8 h-8 bg-indigo-600 rounded-lg flex items-center justify-center"
                            >
                                <span class="text-white font-bold">F</span>
                            </div>
                            <span
                                class="text-xl font-bold text-gray-900 dark:text-white hidden sm:block"
                                >LaricoWallet</span
                            >
                        </a>
                    </div>

                    <!-- Right side: Notifications, Dark Mode, User Menu -->
                    <div class="flex items-center space-x-4">
                        <a
                            href="/notifications"
                            class="relative p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-slate-700"
                        >
                            <svg
                                class="w-6 h-6"
                                fill="none"
                                stroke="currentColor"
                                viewBox="0 0 24 24"
                            >
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M15 17h5l-1.405-1.405A2.032 2.032 0 0118 14.158V11a6.002 6.002 0 00-4-5.659V5a2 2 0 10-4 0v.341C7.67 6.165 6 8.388 6 11v3.159c0 .538-.214 1.055-.595 1.436L4 17h5m6 0v1a3 3 0 11-6 0v-1m6 0H9"
                                ></path>
                            </svg>
                            <span
                                class="absolute top-1 right-1 w-2 h-2 bg-red-500 rounded-full"
                            ></span>
                        </a>
                        <button
                            id="theme-toggle"
                            class="p-2 rounded-lg hover:bg-gray-100 dark:hover:bg-slate-700"
                        >
                            <svg
                                class="w-6 h-6"
                                fill="currentColor"
                                viewBox="0 0 20 20"
                            >
                                <path
                                    d="M17.293 13.293A8 8 0 016.707 2.707a8.001 8.001 0 1010.586 10.586z"
                                ></path>
                            </svg>
                        </button>
                        <!-- User Menu Dropdown -->
                        <div class="relative">
                            <button
                                id="user-menu-button"
                                class="flex items-center space-x-2 hover:bg-gray-100 dark:hover:bg-slate-700 rounded-xl px-3 py-2 transition"
                            >
                                <!-- User Avatar -->
                                {
                                    user.image ? (
                                        <img
                                            src={user.image}
                                            alt="User Avatar"
                                            class="w-8 h-8 rounded-full border-2 border-indigo-500 object-cover"
                                        />
                                    ) : (
                                        <div class="w-8 h-8 bg-gradient-to-r from-indigo-500 to-purple-600 rounded-full flex items-center justify-center text-white font-semibold text-sm">
                                            <span>{initials}</span>
                                        </div>
                                    )
                                }
                                <!-- User Name -->
                                <div
                                    class="hidden sm:flex flex-col items-start"
                                >
                                    <span
                                        class="font-medium text-gray-900 dark:text-white leading-tight"
                                        >{displayName}</span
                                    >
                                    {
                                        isSuperAdmin && (
                                            <span class="text-[10px] font-bold text-white bg-red-600 px-1.5 py-0.5 rounded-full leading-none mt-0.5">
                                                ADMIN
                                            </span>
                                        )
                                    }
                                </div>
                                <!-- Dropdown Icon -->
                                <svg
                                    id="user-menu-icon"
                                    class="w-4 h-4 text-gray-500 dark:text-gray-400 transition-transform"
                                    fill="none"
                                    stroke="currentColor"
                                    viewBox="0 0 24 24"
                                >
                                    <path
                                        stroke-linecap="round"
                                        stroke-linejoin="round"
                                        stroke-width="2"
                                        d="M19 9l-7 7-7-7"></path>
                                </svg>
                            </button>

                            <!-- Dropdown Menu -->
                            <div
                                id="user-dropdown-menu"
                                class="hidden absolute right-0 mt-2 w-56 bg-white dark:bg-slate-800 rounded-xl shadow-lg border border-gray-200 dark:border-slate-700 py-2 z-50"
                            >
                                <div
                                    class="px-4 py-3 border-b border-gray-200 dark:border-slate-700"
                                >
                                    <p
                                        class="text-sm font-semibold text-gray-900 dark:text-white"
                                    >
                                        {displayName}
                                    </p>
                                    <p
                                        class="text-xs text-gray-500 dark:text-gray-400 truncate"
                                    >
                                        {user.email}
                                    </p>
                                </div>

                                {
                                    isSuperAdmin && (
                                        <a
                                            href="/admin"
                                            class="w-full flex items-center space-x-3 px-4 py-2 text-sm text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 transition font-medium"
                                        >
                                            <svg
                                                class="w-5 h-5"
                                                fill="none"
                                                stroke="currentColor"
                                                viewBox="0 0 24 24"
                                            >
                                                <path
                                                    stroke-linecap="round"
                                                    stroke-linejoin="round"
                                                    stroke-width="2"
                                                    d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 110-4m0 4v2m0-6V4"
                                                />
                                            </svg>
                                            <span>Panel Admin</span>
                                        </a>
                                    )
                                }
                                <a
                                    href="/profile"
                                    class="w-full flex items-center space-x-3 px-4 py-2 text-sm text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-slate-700 transition"
                                >
                                    <svg
                                        class="w-5 h-5"
                                        fill="none"
                                        stroke="currentColor"
                                        viewBox="0 0 24 24"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
                                        ></path>
                                    </svg>
                                    <span>Mi Perfil</span>
                                </a>

                                <div
                                    class="border-t border-gray-200 dark:border-slate-700 my-1"
                                >
                                </div>

                                <button
                                    id="logout-btn"
                                    class="w-full flex items-center space-x-3 px-4 py-2 text-sm text-red-600 dark:text-red-400 hover:bg-red-50 dark:hover:bg-red-900/20 transition"
                                >
                                    <svg
                                        class="w-5 h-5"
                                        fill="none"
                                        stroke="currentColor"
                                        viewBox="0 0 24 24"
                                    >
                                        <path
                                            stroke-linecap="round"
                                            stroke-linejoin="round"
                                            stroke-width="2"
                                            d="M17 16l4-4m0 0l-4-4m4 4H7m6 4v1a3 3 0 01-3 3H6a3 3 0 01-3-3V7a3 3 0 013-3h4a3 3 0 013 3v1"
                                        ></path>
                                    </svg>
                                    <span>Cerrar Sesión</span>
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </header>

        <div class="flex">
            <!-- Sidebar -->
            <aside
                id="sidebar"
                class="hidden lg:block w-64 bg-white dark:bg-slate-800 min-h-screen border-r border-gray-200 dark:border-slate-700 fixed lg:sticky top-16 h-[calc(100vh-4rem)] overflow-y-auto z-30"
            >
                <nav class="p-4 space-y-2">
                    <a
                        href="/dashboard"
                        class={`flex items-center space-x-3 px-4 py-3 rounded-xl font-medium transition ${
                            isActive("/dashboard")
                                ? "bg-indigo-50 dark:bg-indigo-900/20 text-indigo-600 dark:text-indigo-400"
                                : "text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-slate-700"
                        }`}
                    >
                        <svg
                            class="w-5 h-5"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6"
                            ></path>
                        </svg>
                        <span>Dashboard</span>
                    </a>
                    <a
                        href="/accounts"
                        class={`flex items-center space-x-3 px-4 py-3 rounded-xl font-medium transition ${
                            isActive("/accounts")
                                ? "bg-indigo-50 dark:bg-indigo-900/20 text-indigo-600 dark:text-indigo-400"
                                : "text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-slate-700"
                        }`}
                    >
                        <svg
                            class="w-5 h-5"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M3 10h18M7 15h1m4 0h1m-7 4h12a3 3 0 003-3V8a3 3 0 00-3-3H6a3 3 0 00-3 3v8a3 3 0 003 3z"
                            ></path>
                        </svg>
                        <span>Cuentas</span>
                    </a>
                    <a
                        href="/transactions"
                        class={`flex items-center space-x-3 px-4 py-3 rounded-xl font-medium transition ${
                            isActive("/transactions")
                                ? "bg-indigo-50 dark:bg-indigo-900/20 text-indigo-600 dark:text-indigo-400"
                                : "text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-slate-700"
                        }`}
                    >
                        <svg
                            class="w-5 h-5"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M9 5H7a2 2 0 00-2 2v12a2 2 0 002 2h10a2 2 0 002-2V7a2 2 0 00-2-2h-2M9 5a2 2 0 002 2h2a2 2 0 002-2M9 5a2 2 0 012-2h2a2 2 0 012 2"
                            ></path>
                        </svg>
                        <span>Transacciones</span>
                    </a>
                    <a
                        href="/categories"
                        class={`flex items-center space-x-3 px-4 py-3 rounded-xl font-medium transition ${
                            isActive("/categories")
                                ? "bg-indigo-50 dark:bg-indigo-900/20 text-indigo-600 dark:text-indigo-400"
                                : "text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-slate-700"
                        }`}
                    >
                        <svg
                            class="w-5 h-5"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M7 7h.01M7 3h5c.512 0 1.024.195 1.414.586l7 7a2 2 0 010 2.828l-7 7a2 2 0 01-2.828 0l-7-7A1.994 1.994 0 013 12V7a4 4 0 014-4z"
                            ></path>
                        </svg>
                        <span>Categorías</span>
                    </a>
                    <a
                        href="/debts"
                        class={`flex items-center space-x-3 px-4 py-3 rounded-xl font-medium transition ${
                            isActive("/debts")
                                ? "bg-indigo-50 dark:bg-indigo-900/20 text-indigo-600 dark:text-indigo-400"
                                : "text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-slate-700"
                        }`}
                    >
                        <svg
                            class="w-5 h-5"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M12 8c-1.657 0-3 .895-3 2s1.343 2 3 2 3 .895 3 2-1.343 2-3 2m0-8c1.11 0 2.08.402 2.599 1M12 8V7m0 1v8m0 0v1m0-1c-1.11 0-2.08-.402-2.599-1M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                            ></path>
                        </svg>
                        <span>Deudas</span>
                    </a>
                    <a
                        href="/invoices"
                        class={`flex items-center space-x-3 px-4 py-3 rounded-xl font-medium transition ${
                            isActive("/invoices")
                                ? "bg-indigo-50 dark:bg-indigo-900/20 text-indigo-600 dark:text-indigo-400"
                                : "text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-slate-700"
                        }`}
                    >
                        <svg
                            class="w-5 h-5"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
                            ></path>
                        </svg>
                        <span>Invoices</span>
                    </a>
                    <a
                        href="/reminders"
                        class={`flex items-center space-x-3 px-4 py-3 rounded-xl font-medium transition ${
                            isActive("/reminders")
                                ? "bg-indigo-50 dark:bg-indigo-900/20 text-indigo-600 dark:text-indigo-400"
                                : "text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-slate-700"
                        }`}
                    >
                        <svg
                            class="w-5 h-5"
                            fill="none"
                            stroke="currentColor"
                            viewBox="0 0 24 24"
                        >
                            <path
                                stroke-linecap="round"
                                stroke-linejoin="round"
                                stroke-width="2"
                                d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"
                            ></path>
                        </svg>
                        <span>Recordatorios</span>
                    </a>
                    <div
                        class="pt-4 mt-4 border-t border-gray-200 dark:border-slate-700"
                    >
                        <a
                            href="/profile"
                            class={`flex items-center space-x-3 px-4 py-3 rounded-xl font-medium transition ${
                                isActive("/profile")
                                    ? "bg-indigo-50 dark:bg-indigo-900/20 text-indigo-600 dark:text-indigo-400"
                                    : "text-gray-700 dark:text-gray-300 hover:bg-gray-100 dark:hover:bg-slate-700"
                            }`}
                        >
                            <svg
                                class="w-5 h-5"
                                fill="none"
                                stroke="currentColor"
                                viewBox="0 0 24 24"
                            >
                                <path
                                    stroke-linecap="round"
                                    stroke-linejoin="round"
                                    stroke-width="2"
                                    d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z"
                                ></path>
                            </svg>
                            <span>Perfil</span>
                        </a>
                    </div>
                </nav>
            </aside>

            <!-- Main Content -->
            <main class="flex-1 p-4 sm:p-6 lg:p-8 min-w-0">
                <slot />
            </main>
        </div>

        <script>
            import { authClient } from "../lib/auth-client";

            // Dark Mode Toggle
            const themeToggle = document.getElementById("theme-toggle");
            if (themeToggle) {
                themeToggle.addEventListener("click", () => {
                    document.body.classList.toggle("dark");
                    localStorage.setItem(
                        "darkMode",
                        document.body.classList.contains("dark")
                            ? "true"
                            : "false",
                    );
                });
            }

            // Load Dark Mode Preference
            if (localStorage.getItem("darkMode") === "true") {
                document.body.classList.add("dark");
            }

            // Mobile Sidebar Toggle
            const mobileMenuBtn = document.getElementById("mobile-menu-btn");
            const sidebar = document.getElementById("sidebar");

            if (mobileMenuBtn && sidebar) {
                mobileMenuBtn.addEventListener("click", () => {
                    sidebar.classList.toggle("hidden");
                    sidebar.classList.toggle("fixed");
                    sidebar.classList.toggle("inset-0");
                    sidebar.classList.toggle("z-50");
                    sidebar.classList.toggle("w-full");
                    sidebar.classList.toggle("bg-white");
                    sidebar.classList.toggle("dark:bg-slate-900");
                });
            }

            // User Dropdown
            const userMenuButton = document.getElementById("user-menu-button");
            const userDropdownMenu =
                document.getElementById("user-dropdown-menu");
            const userMenuIcon = document.getElementById("user-menu-icon");

            if (userMenuButton && userDropdownMenu) {
                userMenuButton.addEventListener("click", (e) => {
                    e.stopPropagation();
                    const isHidden =
                        userDropdownMenu.classList.contains("hidden");
                    if (isHidden) {
                        userDropdownMenu.classList.remove("hidden");
                        userMenuIcon?.classList.add("rotate-180");
                    } else {
                        userDropdownMenu.classList.add("hidden");
                        userMenuIcon?.classList.remove("rotate-180");
                    }
                });

                document.addEventListener("click", (e) => {
                    if (
                        !userMenuButton.contains(e.target as Node) &&
                        !userDropdownMenu.contains(e.target as Node)
                    ) {
                        userDropdownMenu.classList.add("hidden");
                        userMenuIcon?.classList.remove("rotate-180");
                    }
                });
            }

            // Logout - using authClient which properly clears server cookies
            const logoutBtn = document.getElementById("logout-btn");
            if (logoutBtn) {
                logoutBtn.addEventListener("click", async () => {
                    try {
                        // Call better-auth signOut which deletes cookies
                        await authClient.signOut({
                            fetchOptions: {
                                onSuccess: () => {
                                    localStorage.clear();
                                    sessionStorage.clear();
                                    // Force full page reload to /
                                    window.location.href = "/";
                                },
                                onError: (ctx) => {
                                    console.error("Logout error:", ctx.error);
                                    // Even on error, clear storage and redirect
                                    localStorage.clear();
                                    sessionStorage.clear();
                                    window.location.href = "/";
                                },
                            },
                        });
                    } catch (error) {
                        console.error("Logout exception:", error);
                        // Fallback: clear storage and redirect
                        localStorage.clear();
                        sessionStorage.clear();
                        window.location.href = "/";
                    }
                });
            }
        </script>
    </body>
</html>
